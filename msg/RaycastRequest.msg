# frame_id should always be "/world" for VRC
# ..and as we don't want overhead we just drop it for the moment
#std_msgs/Header header

# Ray origin
geometry_msgs/Point32 origin

# Ray direction
geometry_msgs/Point32 direction